apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: twenty-keydb
  namespace: twenty
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: twenty-keydb
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
